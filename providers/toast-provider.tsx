'use client';

import { Toaster } from 'react-hot-toast';

export const ToastProvider = () => {
  return <Toaster />;
};

// Toast notifications require a client-side environment to function properly
